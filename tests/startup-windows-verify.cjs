// Uses a separate temporary Windows startup entry; never modifies the user's companion entry.
const {app}=require('electron'),assert=require('node:assert/strict'),{Startup}=require('../src/startup.cjs');
app.setAppUserModelId('Centerpiece.Companion.StartupVerification');
app.whenReady().then(()=>{const startup=new Startup({isPackaged:true,getLoginItemSettings:o=>app.getLoginItemSettings(o),setLoginItemSettings:o=>app.setLoginItemSettings(o)},process.argv[2]);let changed=false;try{assert.ok(process.argv[2]?.includes(' '));assert.equal(startup.read().enabled,false);changed=true;assert.equal(startup.set(true).enabled,true);assert.equal(startup.read().enabled,true);assert.equal(startup.set(false).enabled,false);console.log('Windows startup enable, persistent readback and disable passed with an installation path containing spaces.');}catch(e){console.error(e);process.exitCode=1;}finally{if(changed)app.setLoginItemSettings({path:process.argv[2],args:['--background'],openAtLogin:false});app.exit(process.exitCode||0);}});
